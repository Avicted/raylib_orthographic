{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with Meson",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/raylib_orthographic",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build debug meson",
            "miDebuggerPath": "/usr/bin/gdb",
        }
    ]
}

